{

    
    "installCommand": "cd Client/metah-viz && npm install && cd ../../api && python3.12 -m pip install -r requirements.txt",

    "buildCommand": "cd Client/metah-viz && npm run build",

    "outputDirectory": "Client/metah-viz/build",
    
    "rewrites": [{ "source": "/(.*)", "destination": "/api/main.py" }]
  }